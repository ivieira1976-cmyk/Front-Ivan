"use strict";
document.addEventListener("DOMContentLoaded",()=>{const user=AgendaAuth.requireLogin();if(!user)return;document.querySelector("#clienteNome").textContent=user.nome;const form=document.querySelector("#formAgendamento"), service=document.querySelector("#servico"), professional=document.querySelector("#profissional"), date=document.querySelector("#data"), time=document.querySelector("#horario"), status=document.querySelector("#bookingStatus");date.min=new Date().toISOString().split("T")[0];[service,professional,date,time].forEach(el=>el.addEventListener("change",renderSummary));renderSummary();renderList();form.addEventListener("submit",e=>{e.preventDefault();const item={id:Date.now().toString(),userId:user.id,servico:service.value,profissional:professional.value,data:date.value,horario:time.value,status:"Confirmado"};const all=AgendaStorage.getAppointments();if(all.some(a=>a.profissional===item.profissional&&a.data===item.data&&a.horario===item.horario)){show("Este horário já foi selecionado nesta demonstração.","error");return}all.push(item);AgendaStorage.saveAppointments(all);show("Agendamento salvo no navegador com sucesso!","success");form.reset();renderSummary();renderList()});function renderSummary(){document.querySelector("#resServico").textContent=service.value||"—";document.querySelector("#resProfissional").textContent=professional.value||"—";document.querySelector("#resData").textContent=date.value?new Date(date.value+"T12:00:00").toLocaleDateString("pt-BR"):"—";document.querySelector("#resHorario").textContent=time.value||"—"}function renderList(){const list=document.querySelector("#appointmentsList");const own=AgendaStorage.getAppointments().filter(a=>a.userId===user.id);list.innerHTML=own.length?own.map(a=>`<article class="appointment-card"><div><strong>${a.servico}</strong><p>${a.profissional} • ${new Date(a.data+"T12:00:00").toLocaleDateString("pt-BR")} às ${a.horario}</p></div><div><span class="badge-status">${a.status}</span><button class="logout-btn cancel-booking" data-id="${a.id}" type="button">Cancelar</button></div></article>`).join(""):"<p>Você ainda não possui agendamentos nesta demonstração.</p>";list.querySelectorAll(".cancel-booking").forEach(btn=>btn.addEventListener("click",()=>{AgendaStorage.saveAppointments(AgendaStorage.getAppointments().filter(a=>a.id!==btn.dataset.id));renderList()}))}function show(text,type){status.textContent=text;status.className=`status-message show ${type}`}});
