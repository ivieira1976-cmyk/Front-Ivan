"use strict";
const AgendaStorage={keys:{users:"agendapro_users",session:"agendapro_session",appointments:"agendapro_appointments"},read(key,fallback=[]){try{return JSON.parse(localStorage.getItem(key))??fallback}catch{return fallback}},write(key,value){localStorage.setItem(key,JSON.stringify(value))},getUsers(){return this.read(this.keys.users,[])},saveUsers(v){this.write(this.keys.users,v)},getSession(){return this.read(this.keys.session,null)},setSession(v){this.write(this.keys.session,v)},clearSession(){localStorage.removeItem(this.keys.session)},getAppointments(){return this.read(this.keys.appointments,[])},saveAppointments(v){this.write(this.keys.appointments,v)}};window.AgendaStorage=AgendaStorage;
